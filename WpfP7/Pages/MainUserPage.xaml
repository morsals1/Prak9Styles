<Page x:Class="WpfP7.Pages.MainUserPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="MainUserPage"
      Background="{DynamicResource AppBackground}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Информация о докторе">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding CurrentDoctor.LastName}"/>
                <TextBlock Text="{Binding CurrentDoctor.Name}"/>
                <TextBlock Text="{Binding CurrentDoctor.MiddleName}"/>
                <TextBlock Text="-"/>
                <TextBlock Text="{Binding CurrentDoctor.Specification}"/>
                <TextBlock Text="ID:"/>
                <TextBlock Text="{Binding CurrentDoctor.Id}"/>
            </StackPanel>
        </GroupBox>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Style="{StaticResource HorizontalStackPanel}">
            <Button Style="{StaticResource AddButton}" 
                    Content="Создать пациента" 
                    Click="CreatePatientButton_Click"/>
            <Button Style="{StaticResource StandardButton}" 
                    Content="Начать прием" 
                    Click="StartAppointmentButton_Click"/>
            <Button Style="{StaticResource StandardButton}" 
                    Content="Изменить информацию" 
                    Click="EditInfoButton_Click"/>
            <Button Style="{StaticResource DeleteButton}"
                    Content="Удалить пациента"
                    Click="DeleteButton_Click"/>
        </StackPanel>

        <ListView Grid.Row="2" 
                  ItemsSource="{Binding Patients}" 
                  SelectedItem="{Binding SelectedPatient}"
                  MouseDoubleClick="PatientList_DoubleClick">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Background="{DynamicResource ButtonBackground}" 
                            CornerRadius="5" 
                            Padding="10">
                        <StackPanel>
                            <TextBlock Text="{Binding LastName}"/>
                            <TextBlock Text="{Binding Name}"/>
                            <TextBlock Text="{Binding MiddleName}"/>
                            <TextBlock Text="{Binding PhoneNumber}"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>